{
  "name": "wibbly-world",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "rm -rf built && babel background.js --out-file built.js && mkdir built  && cp built.js built/ && cp manifest.json built/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel": "^6.1.18",
    "babel-cli": "^6.2.0",
    "babel-preset-es2015": "^6.1.18"
  }
}
